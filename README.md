# 推理服务路由

本项目旨在提供一个极简、高性能的推理路由。具有下列特性：

- 模型路由：根据传入请求的模型将请求路由到指定推理后端；
- 协议透传：支持 OpenAI 和 Anthropic 协议，支持在两种协议间转换，但默认行为是将请求原封不动地传递到后端以获得最优性能；
- 负载均衡：支持为同一个模型配置多个推理后端，并在这些后端之间按规则选择；在某个模型所有后端都不可用时将请求引导到指定备份模型；
